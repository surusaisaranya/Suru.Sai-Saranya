#include <stdio.h>
void main()
{
char x;
printf (" enter the character to check:");
scanf ("%c",&x);
if (x>='a'&& x<='z' || x>='A' && x<='Z')
{
printf ("%c is a character",x);
}
else
{
printf ("%c is not a character",x);
}
